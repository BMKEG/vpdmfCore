language: java
jdk:
  - openjdk6
before_install:
  - mysql -u travis -e "CREATE USER 'build'@'localhost' IDENTIFIED BY 'LP-U88ht';"
  - mysql -u travis -e "GRANT ALL PRIVILEGES ON * . * TO 'build'@'localhost';"
  - mysql -u travis -e "FLUSH PRIVILEGES;"
  - git clone https://github.com/BMKEG/bmkeg-parent ../bmkeg-parent
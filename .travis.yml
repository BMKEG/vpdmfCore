language: java
jdk:
  - openjdk6
before_install:
  - mysql -e "CREATE USER 'build'@'localhost' IDENTIFIED BY 'LP-U88ht';"
  - mysql -e "GRANT ALL PRIVILEGES ON * . * TO 'build'@'localhost';"
  - mysql -e "FLUSH PRIVILEGES;"
  - git clone https://github.com/BMKEG/bmkeg-parent ../bmkeg-parent